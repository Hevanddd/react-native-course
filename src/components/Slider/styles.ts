import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  sliderWrapper: {
    marginTop: 43,
    height: 288,
  },
  image: {
    height: 250,
    width: 250,
  },
  imageWrapper: {
    alignItems: 'center',
  },
});

export default styles;
